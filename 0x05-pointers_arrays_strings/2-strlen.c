/**
 * main - check the code
 * @s: char to check
 * Description: this will return the length of a string
 * Return: Always 0.
 */
int _strlen(char *s)
{
int a;
for (a = 0; a < *s++; a++)
{
return (a);
}

